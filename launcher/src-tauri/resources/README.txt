# Populated by the client-mod Gradle build (`gradle build` runs
# installToLauncher, which drops duskclient-<mc>.jar here). The launcher ships
# it as a Tauri resource and force-loads it into Fabric instances.
