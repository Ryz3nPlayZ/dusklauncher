// The previous frontend was removed on 2026-09-09; the full UI (views,
// components, stores, IPC wrappers, design CSS) lives in git history under
// "Checkpoint UI state before frontend reset". The next UI starts here.
// Scene background assets survive in ./assets/background/.
export {};
