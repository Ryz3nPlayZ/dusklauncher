{
  "schemaVersion": 1,
  "id": "duskclient",
  "version": "${version}",
  "name": "DuskClient",
  "description": "Dusk cosmetics (capes, accessories) and PvP HUD modules.",
  "authors": ["Dusk"],
  "license": "GPL-3.0-or-later",
  "environment": "client",
  "entrypoints": {
    "client": ["dev.dusk.client.DuskClient"]
  },
  "mixins": [
    "duskclient.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=0.17.0",
    "java": "${java}",
    "fabric-api": "*",
    "minecraft": "${mc_range}"
  }
}
